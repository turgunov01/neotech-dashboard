import type { TraitsInterface } from "../interface/traits";

export class Traits {
    private traits: TraitsInterface;

    constructor(traits: TraitsInterface) {
        this.traits = traits
    }
}